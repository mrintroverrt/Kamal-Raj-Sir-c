#include <stdio.h>
int main()
{
    int a = 10;
    int b = 20;
    int c = 40;
    if (a < b)
    {
        printf("Yes a is < than b");
    }
    else if (a == c)
    {
        printf("Yes a is Equal to c");
    }
    else
    {
        printf("No a is not less than b");
    }
    return 0;
}