#include <stdio.h>

int main(void)
{
    int X, Y, A;

    while (Y < A)
    {
        if (A < X)
        {
            printf("Yes");
        }
        else if (Y < A)
            else
            {
                printf("No")
            }
        return 0;
    }
}